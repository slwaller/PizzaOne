.jumbotron
  %center
    = form_for [@company, @deal] do |f|
      - if @deal.errors.any?
        #error_explanation
          %h2= "#{pluralize(@deal.errors.count, "error")} prohibited this deal from being saved:"
          %ul
            - @deal.errors.full_messages.each do |msg|
              %li= msg
      .form-label
        = f.label :avatar
      .form-group
        = f.file_field :avatar, class: "form-control"
      .form-label
        = f.label :name
      .form-group
        = f.text_field :name, class: "form-control", placeholder: "Please input the name of your menu item"
      .form-label
        = f.label :description
      .form-group
        = f.text_area :description, class: "form-control", placeholder: "Nutrition, Toppings, Size"
      .form-label
        = f.label :price
      .form-group
        = f.number_field :price, class: "form-control", placeholder: "$XX.XX", :step => 0.01
      .action
        = f.submit "Submit", class: "btn btn-lg btn-warning"
